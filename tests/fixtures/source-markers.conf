# configuration file /etc/nginx/http.conf:
http {
  server {
    listen 80;
    server_name source.example.test;

    location / {
      proxy_pass http://source_pool;
    }
  }
}

# configuration file /etc/nginx/upstreams.conf:
upstream source_pool {
  server 10.0.0.50:8080;
}
