http {
  upstream duplicate_pool {
    server 10.0.0.60:8080;
    server 10.0.0.60:8080;
  }

  server {
    listen 80;
    server_name duplicate.example.test;

    location / {
      proxy_pass http://duplicate_pool;
    }
  }
}
