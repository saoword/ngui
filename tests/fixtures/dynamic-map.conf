http {
  map $host $backend {
    default app_pool;
  }

  upstream app_pool {
    server 10.0.0.30:8080;
  }

  server {
    listen 80;
    server_name dynamic.example.test;

    location /api {
      proxy_pass http://$backend;
    }
  }
}
