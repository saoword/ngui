http {
  server {
    listen 80;
    server_name rewrite.example.test;

    location /old {
      rewrite ^/old/(.*)$ /new/$1 permanent;
    }

    location / {
      try_files $uri /index.html;
    }
  }
}
